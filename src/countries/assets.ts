/**
 * The default portrait and the generator card templates live in `public/` so
 * they stay out of the JS bundle — they are large, and only the card generation
 * path ever reads them.
 */
export const DEFAULT_PORTRAIT_URI = '/templates/default-portrait.jpg';
